To install required python packages run `pip install -r requirements.txt`

In the same folder as the notebook should be a folder called "Data". Within that folder should be the unzipped data(so this should contain the folders named "hrv", "vragenlijst" etc.).